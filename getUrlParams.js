const urls = window.location.href;
const data = new URL(urls)?.pathname;

const url = window.location.href;
const urlString = new URL(url);
const keyword = urlString.searchParams?.get("keyword");

const getUrlParams = () => {
  
}

const pageUrl = "https://this-page.com/about?name=my-name&aka=my-aka"

console.log("urllll", )